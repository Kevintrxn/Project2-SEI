<!DOCTYPE html>
<html>
    <head>
        <title>Edit Event - venue.Me</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
<% include partials/header %>
    <body>
    <h1>Edit Event</h1>
    <form action="/venue/events/edit/<%= event._id %>?_method=PUT" method="post">
        <input type="text" name="eventName" value="<%= event.eventName %>" placeholder="Event Name" required>
        <input type="text" name="eventLocation" value="<%= event.eventLocation %>" placeholder="Event Location" required>
        <input type="date" name="eventDate" value="<%= event.eventDate.toISOString().split('T')[0] %>" required>
        <select name="experience" required>
        <option value="positive" <%= event.experience === 'positive' ? 'selected' : '' %>>Positive</option>
        <option value="negative" <%= event.experience === 'negative' ? 'selected' : '' %>>Negative</option>
    </select>
        <input type="number" name="rating" value="<%= event.rating %>" min="0" max="5" required>
        <textarea name="reviewText" placeholder="Review Text" required><%= event.reviewText %></textarea>
        <button type="submit">Update Event</button>
    </form>
    </body>
</html>
